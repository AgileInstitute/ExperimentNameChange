apply plugin: 'java'
apply plugin: 'eclipse'

defaultTasks 'intro'

dependencies {
	testCompile group: 'junit', name: 'junit', version: '4.10'
}

task intro << {
	printLn 'Gradle build script running...'
}

task myNonsense(dependsOn: intro) << {
}
